// @ts-nocheck
const HelloWorld = ({ awsId }: any) => {
    return (
        <div className={`border`} another={`border`} style={`border`}>
            Hello, world!
        </div>
    );
};

export default HelloWorld;
